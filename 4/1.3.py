tags = {
    "Django" if tag.lower() == "Django" else tag.lower()
    for tag in [
        "Django",
        "FastAPI",
        "Numpy",
        "PYTHON",
        "Pandas",
        "FASTAPI",
        "Python",
        "random",
    ]
}
print(tags)
